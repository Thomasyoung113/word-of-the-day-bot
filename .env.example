TELEGRAM_BOT_TOKEN=your_token_here
BYNARA_API_KEY=your_bynara_api_key_here
BYNARA_BASE_URL=https://router.bynara.id/v1
BYNARA_MODEL=mistral-medium-3-5
ALLOWED_CHAT_IDS=
# Optional: directory for state.json. Leave unset for local dev (uses the
# project folder). On Render, set this to your mounted disk's path (see
# render.yaml, which sets it to /var/data automatically).
STATE_DIR=
